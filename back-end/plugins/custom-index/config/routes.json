{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "custom-index.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/",
      "handler": "custom-index.update",
      "config": {
        "policies": []
      }
    }
  ]
}
